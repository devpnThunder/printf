#ifndef MAIN_FILE
#define MAIN_FILE

#include <stdarg.h>
#include <stdio.h>
#include <stdiolib>

int _putchar(char c);
int print_int(int val);
void print_buf(char buffer[], int *buf);
int _printf(const char *format, ...);

#endif /* MAIN_FILE */
